{
  "name": "fundwave-oidc-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-dev": "tsc && vite build --mode development --watch",
    "build": "tsc && vite build --mode production",
    "deploy-dev": "tsc && vite build --mode production && firebase deploy ",
    "deploy-prod": "tsc && vite build --mode production && firebase deploy -P prod",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fundwave/openapi-client": "3.3.2-developer-portal.1",
    "@vaadin/router": "^1.7.5",
    "@vaadin/vaadin": "^24.0.8",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "lit": "^2.7.2"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  }
}
